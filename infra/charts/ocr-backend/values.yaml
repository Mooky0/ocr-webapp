replicaCount: 1

image:
  repository: "ghcr.io/mooky0/ocr-backend"
  tag: "a8e34eb"

ocrWorker:
  image:
    repository: "ghcr.io/mooky0/ocr-worker"
    tag: "a8e34eb"

service:
  port: 8000

env:
  dbUrl: "postgresql://user:pass@postgres:5432/ocrdb"
  minioUrl: "minio:9000"
  minioRootUser: "admin"
  minioRootPassword: "password"
  kafkaBootstrapServers: "kafka:9092"